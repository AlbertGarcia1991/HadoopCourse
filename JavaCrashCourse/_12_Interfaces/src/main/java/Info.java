// Interface is a way of forcing to any class (that implements the interface) to have some common
// methods. A class can implement as many Interfaces as desired, always separing them by commas
public interface Info {
    // Interfaces do not have code inside methods, so no curly braces
    public void showInfo();
}
